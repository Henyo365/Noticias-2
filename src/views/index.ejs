<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Noticias</title>
    <link rel="stylesheet" type="text/css" href="../public/css/main.css">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@100;200;300;400;500;600;700;800;900&display=swap" rel="stylesheet">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <%- include("partials/head") -%>
</head>

<body>
    

    <%- include("partials/navigation") %>
    <header class="header__box">
        <nav class="header__nav">
            <li class="header__nav_list">
                <ul><a class="list__item" href="/">INICIO</a></ul>
                <ul><a class="list__item"  href="/libros">LIBROS</a>	</ul>
                <ul><a  class="list__item" href="/news">OTRAS NOTICIAS</a>	</ul>
            </li>			
        </nav>
    </header>

    <div class="container p-4">
        <div class="row">
            <div class="main__box__title">
                <h1 class="main__title">Noticias del Día</h1>
            </div>
            <div class="card-columns">
                

                <% images.forEach(function (image) { %>

                <div class="card animated fadeInDown">
                    <img src="<%= image.path %>" class="card-img-top" alt="<%= image.filename %>">
                    <div class="card-body">
                        <h5 class="card-title"><%= image.title %></h5>
                        <p class="card-text"><%= image.description %></p>
                        <p><%= format(image.created_at) %></p>
                        <a href="/image/<%= image._id %>" class="btn btn-primary">
                            Ver
                        </a>
                    </div>
                </div>
                <% }) %>
            </div>
        </div>
    </div>
    <footer class="footer__box">
		<div class="footer__link">
			<a href="http://www.unefa.edu.ve/portal/" class="footer__link__texto" target="_blank">Pagina oficial de la universidad</a>
		</div>
	</footer>

</body>

</html>